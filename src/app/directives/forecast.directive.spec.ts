import { ForecastDirective } from './forecast.directive';

describe('ForecastDirective', () => {
  it('should create an instance', () => {
    const directive = new ForecastDirective();
    expect(directive).toBeTruthy();
  });
});
