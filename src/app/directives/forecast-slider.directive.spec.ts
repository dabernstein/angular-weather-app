import { ForecastSliderDirective } from './forecast-slider.directive';

describe('ForecastSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ForecastSliderDirective();
    expect(directive).toBeTruthy();
  });
});
