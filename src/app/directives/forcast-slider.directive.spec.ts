import { ForcastSliderDirective } from './forcast-slider.directive';

describe('ForcastSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ForcastSliderDirective();
    expect(directive).toBeTruthy();
  });
});
